format string is a kind of vulnerability where the format string function interprints the entered input as the command. this happens when the format string has no specifiers.. when the user inputs data, the format function will treat the data as the commands. 

**writing to memory using format strings in a format string vulnerability** 
in some exploits, we need to write and call an address that is on the memory of the stack.. this is kind of complex than writing just the address to the memory 