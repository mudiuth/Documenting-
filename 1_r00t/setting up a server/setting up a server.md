Every machine should have a an identity.
Naming is important in Information security.
to give the computer an ip address, we use the command `ncpa.cpl` this will open a configuration menu and then select the IP_version to use
teh first control in the network security is network segmentation.  After setting up the server, it should be on a different network far from the other.. itd be useful for the server to have its network alone 


server manager -> active directory users and computers -> server manager tools -> navigate to the domain  -> right click on the domain -> go to new -> give it a name -> set a password 
after add a computer and give it to a user 

for policies. na


tools.. group policy management .. domain name ...  create a GPO -- give it a name -- (eg. lock USB) -- right click and enforce it --  right click and edit -- 

control panel and usb are some of the policies that are cruicial to enforce. this can help act as a mitigation factor from number of vulnerabilities. 

implementing identity and access management 
